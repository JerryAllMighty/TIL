[개요]
- 오늘은 엔티티 설계하면 생성자를 만드는 패턴들에 대해서 비교해보고,
각 장단점을 분석해서 정리하고 적용해보았다. 아래와 같다. 보도록!

------------
[고민 포인트]
엔티티를 생성함에 있어서 어떤 방법으로 생성할지에 대한 고민

[public new]

불필요한 객체 생성
생성 파라미터에 따라 가변적으로 생성 필요
[buidler]

코드 중복 해소. 각 생성 파라미터가 아주 가변적이라면 빌더 패턴을 통해 코드 중복 해소와 가독성 향상 가능
[static factory method pattern]

메서드 명을 활용 가능해 해당 엔티티의 생성 의도를 명확하게 표현 가능
생성 파라미터가 가변적이지 않아 매번 메서드 만들어줘야함
[결론]

나는 상황상 매번 생성 파라미터가 가변적으로 많이 바뀔 것 같지 않아 정적 팩토리 메서드 패턴을 활용하기로 결정고민 포인트]
엔티티를 생성함에 있어서 어떤 방법으로 생성할지에 대한 고민
[public new]

불필요한 객체 생성
생성 파라미터에 따라 가변적으로 생성 필요
[buidler]

코드 중복 해소. 각 생성 파라미터가 아주 가변적이라면 빌더 패턴을 통해 코드 중복 해소와 가독성 향상 가능
[static factory method pattern]

메서드 명을 활용 가능해 해당 엔티티의 생성 의도를 명확하게 표현 가능
생성 파라미터가 가변적이지 않아 매번 메서드 만들어줘야함
[결론]

나는 상황상 매번 생성 파라미터가 가변적으로 많이 바뀔 것 같지 않아 정적 팩토리 메서드 패턴을 활용하기로 결정
** JPA 사용시 프록시 클래스를 활용하여야 하기에 기본 생성자는 반드시 필요.
하지만 public으로 하여 불필요한 객체 생성은 피하고, private은 객체 생성이 밖에서 안되기에 protected로 활용


[참고]
https://github.com/JerryAllMighty/code-shop/issues/4