[자바의 타입 (자료형)]
- 기본 자료형
> 8가지
byte, short, int, long, char, boolean, double (64 bit), float (32 bit)
바로 초기화
- 참조 자료형
new 로 초기화
String은 예외라서 new로 초기화 안 해도 가능

[기본값]
- 지역 변수로 자료형을 사용할 때에는 기본 값이 자동으로 적용되지 않고 반드시 값을 지정해야만 한다
- 변수의 값이 기본값이라고 할지라도, 명시적으로 기본값을 지정해주는 것이 좋다
- 기본값을 할당하는 코딩 버릇을 들이는 것이 좋다
