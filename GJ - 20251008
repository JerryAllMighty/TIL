[배열의 기본값]
- 배열에서는 지역 변수라고 할지라도, 배열의 크기만 정해주면 문제가 발생하지 않는다
- 기본 자료형 배열의 기본값은 각 자료형의 기본값과 동일하다
- 모든 참조 자료형은 호기화를 하지않으면 null이다
- 배열에서는 지역 변수라고 할지라도, 배열의 크기만 정해주면 문제가 발생하지 않는다
- 기본 자료형 배열의 기본값은 각 자료형의 기본값과 동일하다
- 모든 참조 자료형은 호기화를 하지않으면 null이다
